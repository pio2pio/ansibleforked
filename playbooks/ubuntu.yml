---

- hosts: localhost
  become: yes
  become_method: sudo
  connection: local
  gather_facts: yes

  pre_tasks:
  - debug:
      msg: "Any prereq tasks go here"

  roles:
  - { role: prereq }
  - { role: general }
  - { role: vim }
  - { role: developer } 

