---

- hosts: retropi
  become: yes
  become_method: sudo
  connection: local
  gather_facts: yes

  vars:
    - gui: False
      root_user:
        - name: 'root'
          home: '/root'
      primary_user:
        - name: 'pi'
          home: '/home/pi'
      all_users:
        - '{{ root_user }}'
        - '{{ primary_user }}'

  pre_tasks:
    - debug:
        msg: "Any prereq tasks go here"

  roles:
    - { role: prereq }
    - { role: vim }
    - { role: powerline-shell }

