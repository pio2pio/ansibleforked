---

- name: "libraries | apt"
  apt:
    name: "{{ item }}"
    state: "latest"
  with_items: "{{ libraries_apt }}"

- name: "libraries | pip"
  pip:
    name: "{{ item }}"
    state: "latest"
  with_items: "{{ libraries_pip }}"

